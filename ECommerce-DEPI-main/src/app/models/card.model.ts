export interface Card {
  id: number;
  name: string;
  price: number;
  description: string;
  imageUrl: string;
  category: string;
} 
/* this is the structure of each product content in product card */